install.packages(c("jsonlite", "dplyr", "knitr", "rmarkdown"))
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
library(jsonlite)
library(dplyr)
library(knitr)
# Adjust the path below if your .Rmd lives somewhere other than
# the same folder as data.json
data <- fromJSON("data/data_sources.json", flatten = TRUE)
getwd()
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
library(jsonlite)
library(dplyr)
library(knitr)
# Adjust the path below if your .Rmd lives somewhere other than
# the same folder as data.json
data <- fromJSON("data/data_sources.json", flatten = TRUE)
raw <- readLines("data/data_sources.json", encoding = "UTF-8", warn = FALSE)
json_text <- paste(raw, collapse = "\n")
data <- jsonlite::fromJSON(json_text, flatten = TRUE)
View(data)
# Adjust the path below if your .Rmd lives somewhere other than
# the same folder as data.json
raw <- readLines("data/data_sources.json", encoding = "UTF-8", warn = FALSE)
data <- fromJSON("data/data_sources.json", flatten = TRUE)
knitr::opts_chunk$set(echo = FALSE, message = FALSE, warning = FALSE)
library(jsonlite)
library(dplyr)
library(knitr)
library(here)
# Adjust the path below if your .Rmd lives somewhere other than
# the same folder as data.json
data <- fromJSON(here("data", "data_sources.json"), flatten = TRUE)
# raw <- readLines("data/data_sources.json", encoding = "UTF-8", warn = FALSE)
# json_text <- paste(raw, collapse = "\n")
# data <- jsonlite::fromJSON(json_text, flatten = TRUE)
# datasets <- data$datasets
# Some fields (institution, source, linkURL, contactInfo) can be
# either a plain string or an array depending on the entry.
# Collapse any list-columns into single comma-separated strings.
datasets <- datasets %>%
mutate(across(where(is.list), ~ sapply(., function(x) {
if (length(x) == 0 || all(x == "")) return("")
paste(x, collapse = ", ")
})))
# Adjust the path below if your .Rmd lives somewhere other than
# the same folder as data.json
data <- fromJSON(here("data", "data_sources.json"), flatten = TRUE)
datasets <- data$datasets
# Some fields (institution, source, linkURL, contactInfo) can be
# either a plain string or an array depending on the entry.
# Collapse any list-columns into single comma-separated strings.
datasets <- datasets %>%
mutate(across(where(is.list), ~ sapply(., function(x) {
if (length(x) == 0 || all(x == "")) return("")
paste(x, collapse = ", ")
})))
# Recode the 0/1 flags as Yes/No for readability
datasets <- datasets %>%
mutate(
isCovid    = ifelse(isCovid == 1, "Yes", "No"),
isBelgium  = ifelse(isBelgium == 1, "Yes", "No"),
isActive   = ifelse(isActive == 1, "Yes", "No")
)
datasets %>%
select(
ID = id,
Name = name,
Type = type,
Institution = institution,
Category = category,
`Detail Level` = detailLevel,
Accessibility = accessibility,
`Update Frequency` = updateFrequency,
`COVID-specific` = isCovid,
`Belgium-specific` = isBelgium,
Active = isActive
) %>%
kable()
datasets %>%
select(
ID = id,
Name = name,
Type = type,
Institution = institution,
Category = category,
`Detail Level` = detailLevel,
Accessibility = accessibility,
`Update Frequency` = updateFrequency,
`COVID-specific` = isCovid,
`Belgium-specific` = isBelgium,
Active = isActive
) %>%
kable()
datasets %>%
select(
Name = name,
`Short Description` = shortDescription,
Source = source,
Link = linkURL
) %>%
kable()
